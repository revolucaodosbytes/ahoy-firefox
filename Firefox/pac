function FindProxyForURL(url, host)
{

// Use Proxy?
if (host == "toppt.net" || host == "www.toppt.net")
  { return 'PROXY proxy1.ahoy.pro:3128'; }
  
  if (host == "omeuip.com" || host == "www.omeuip.com")
  { return 'PROXY proxy1.ahoy.pro:3128'; }
  
  return direct; 
}